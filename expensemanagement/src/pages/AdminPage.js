import { Typography, Box } from '@mui/material';

const AdminPage = () => (
  <Box sx={{ py: 6, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>
      Admin Panel
    </Typography>
    <Typography variant="body1">Administrative tools and analytics.</Typography>
  </Box>
);

export default AdminPage;
